<div class="col-md-12 bg-info close">
	<p> <%= action[:msg] %>
	<span>
	  <% unless action[:url].blank? %>
		<%= link_to("paste", action[:url], options= {method: :post}) %>
		|
      <% end %>    
		<%= link_to("cancel", cancel_path) %>
	</span>
</div>
